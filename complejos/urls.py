from django.conf import settings
from django.conf.urls import patterns, include, url

urlpatterns = patterns("",
    #url(r'^torneo/agregar$','complejos.views.agregar', name='agregar_torneo'),
    #url(r'^torneo/editar/$','complejos.views.editar', name='editar_torneo'),
    #url(r'^torneo/editar/(?P<id>\d+)/$','complejos.views.editar', name='editar_torneo'),
    #url(r'^torneo/ver/(?P<id>\d+)/$','complejos.views.ver', name='ver_torneo'),
    
)